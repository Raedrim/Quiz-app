import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'raedrim.quizapp',
  appName: 'quiz-app',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
